import 'package:flutter/material.dart';

class AppColors {
  // Primary (네이비)
  static const Color primary = Color(0xFF1A2B5E);
  static const Color primaryDark = Color(0xFF0F1A3D);
  static const Color primaryLight = Color(0xFF2E4A8E);

  // Secondary (골드)
  static const Color secondary = Color(0xFFC9960C);
  static const Color secondaryDark = Color(0xFF9A7108);
  static const Color secondaryLight = Color(0xFFE8B832);

  // Accent (골드 밝음)
  static const Color accent = Color(0xFFE8B832);

  // Neutral
  static const Color background = Color(0xFFF8F8F8);
  static const Color surface = Color(0xFFFFFFFF);
  static const Color error = Color(0xFFD32F2F);

  // Text
  static const Color textPrimary = Color(0xFF333333);
  static const Color textSecondary = Color(0xFF666666);
  static const Color textHint = Color(0xFF999999);
}
